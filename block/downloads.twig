{% if is_preview %}
  <p><strong>Downloads</strong> - I will only appear in the editor.</p>
{% endif %}

{% if not is_preview %}
<div class="container-wide grid grid-cols-1 grid-rows-2 sm:grid-cols-2 relative" >
  <div class="row-span-2 hidden sm:block">
    <img src="{{ fields.image.url }}" alt="">
  </div>
  <div class="flex items-center p-6 bg-pop-{{ fields.accent_color }}">
    <h3 class="text-pop-white">
      {{ fields.title }}
    </h3>
  </div>
  <div class="flex flex-col bg-pop-white">
    <div class="flex-grow p-6">
      {{ fields.text }}
    </div>
    <div class="self-end">
      {% include "/templates/partial/link.twig" with 
        {
          'link': fields.link,
        }
      %}
    </div>
  </div>        
</div>
{% endif %}
