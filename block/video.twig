{% if is_preview %}
  <p><strong>Text Block</strong> - I will only appear in the editor.</p>
{% endif %}

{% if not is_preview %}
  <div class="container-narrow relative flex flex-wrap md:flex-nowrap" >
    <div class="w-full embed-container">
      {{ fields.video }}
    </div>
  </div>
{% endif %}